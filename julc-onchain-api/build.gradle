plugins {
    id 'java-library'
}

description = 'On-chain API: annotations and stdlib stubs for IDE support'

dependencies {
    api project(':julc-core')         // for PlutusData type (transitive to users)
    api project(':julc-ledger-api')   // for ledger types (TxInfo, TxOut, etc.)
}
