package com.bloxbean.cardano.julc.onchain.ledger;

/**
 * A bound of a time interval, with a type and inclusiveness flag.
 * <p>
 * This is an IDE stub. The compiler uses the schema from LedgerTypeRegistry.
 */
public record IntervalBound(IntervalBoundType boundType, boolean isInclusive) {}
