name: Publish Snapshot

on:
  workflow_dispatch:

jobs:
  publish-snapshot:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 25
      uses: actions/setup-java@v4
      with:
        java-version: '25'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4

    - name: Build
      run: ./gradlew clean build -PskipSigning=true --stacktrace

    # `publish` no longer reaches a remote: the OSSRH staging repository went
    # away with nexusPublishing, and the Central Portal snapshot endpoint is
    # driven by nmcp's aggregation task instead.
    - name: Publish to Snapshot Repository
      run: ./gradlew publishAggregationToCentralPortalSnapshots -PskipSigning=true --stacktrace
      env:
        MAVEN_USERNAME: ${{ secrets.OSSRH_USERNAME }}
        MAVEN_PASSWORD: ${{ secrets.OSSRH_TOKEN }}
